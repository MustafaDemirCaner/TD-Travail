def capitaliser(mot):
    """retourne la chaine dont toutes minuscules ont été remplacé par les majuscules correpondantes.
    :param mot: (str).
    :return : (str) la chaine dont toutes minuscules ont été remplacé par les majuscules
    ,→ correpondantes."""
    return mot.upper()

print(capitaliser("bonjour"))
